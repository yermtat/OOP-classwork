#include<iostream>

using namespace std;

int main() {
	// cin - istream object (bufferized) >>
	// cout - ostream object (bufferized) << (побитовое смещение)
	// cerr - ostream obejct (non-bufferized) - чтобы выводить на консоль сообщения об ошибках
	// clog - ostream object (bufferized) - тоже чтобы выводить сообщения об ошибках, но буфферизованно
}